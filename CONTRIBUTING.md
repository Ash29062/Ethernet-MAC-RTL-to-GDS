# Contributing

- Keep RTL license headers intact in reused files.
- Do not commit large generated GDS outputs.
- Prefer notebook-based changes in `final/` for reproducible flow updates.
- Document any new scripts or flow parameters in `metadata/` or `Doc/`.
