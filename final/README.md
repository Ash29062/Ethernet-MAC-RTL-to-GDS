# Final Flow

This folder contains the notebook-driven implementation flow for synthesis and physical design.

Recommended usage:
1. Enter the LibreLane environment with `nix-shell`.
2. Start Jupyter inside the shell.
3. Open the notebook and run cells in order.

HTML files in this folder are static snapshots of completed runs.
